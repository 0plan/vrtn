import { t } from 'i18next'
import { useEffect, useState } from 'react'

const menuItems = () => {
  const menus =
  const [menu, setMenu] = useState(menus)

  useEffect(() => {
    setMenu(menus)
  }, menu)
  return menu

}

export {
  menuItems
}
